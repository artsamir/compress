# 🚀 Quick Start - Chatbot Integration

আপনার CutCompress সাইটে AI Chatbot integrate করা হয়েছে!

## 🎯 কী যোগ হয়েছে?

### 1. **Chatbot Widget** 💬
- আপনার সাইটের প্রতিটি পেজে নীচে ডান কোণে একটি চ্যাট বাটন
- ব্যবহারকারীরা সরাসরি সাইট সম্পর্কে প্রশ্ন করতে পারে
- মেশিন লার্নিং AI দ্বারা চালিত (Mistral/Llama2)

### 2. **Ollama AI Model** 🤖
- সম্পূর্ণ free এবং offline
- 7B প্যারামিটার মডেল (দ্রুত এবং সঠিক)
- আপনার প্রাইভেট সার্ভারে চলে (ডেটা শেয়ার করা হয় না)

### 3. **নতুন Files যোগ করা হয়েছে**
- `blueprints/chatbot_api.py` - API এন্ডপয়েন্ট
- `templates/chatbot_widget.html` - UI ইন্টারফেস
- `CONTABO_CHATBOT_GUIDE.md` - বিস্তারিত Contabo setup গাইড
- `deploy_contabo.sh` - Automated deployment script
- `config.py` - Configuration management

## 🏃 দ্রুত শুরু (Local Testing)

```bash
# ১. Virtual environment activate করুন
source venv/bin/activate  # Linux/Mac
# or
.\venv\Scripts\Activate  # Windows

# ২. প্রয়োজনীয় প্যাকেজ ইনস্টল করুন
pip install -r requirements.txt

# ৩. Ollama ডাউনলোড করুন
ollama pull mistral

# ৪. দুটি টার্মিনালে চালান:
# Terminal 1: Ollama সার্ভার
ollama serve

# Terminal 2: Flask অ্যাপ
python application.py

# ৫. ব্রাউজার খুলুন
http://localhost:5000
```

## 🌐 Contabo VPS তে ডিপ্লয়মেন্ট

একটি single command দিয়ে সবকিছু setup হবে:

```bash
ssh root@your-vps-ip

# Linux তে ডাউনলোড করুন এবং চালান
curl -O https://raw.githubusercontent.com/artsamir/compress/main/deploy_contabo.sh
chmod +x deploy_contabo.sh
./deploy_contabo.sh cutcompress.com
```

**বা ম্যানুয়ালি:**

```bash
# বিস্তারিত ধাপের জন্য CONTABO_CHATBOT_GUIDE.md পড়ুন
cat CONTABO_CHATBOT_GUIDE.md
```

## 🧠 Chatbot কীভাবে কাজ করে?

```
ব্যবহারকারী "এই সাইটে কী করতে পারি?"
        ↓
   Chatbot Widget
        ↓
   /api/chatbot/ask (Flask)
        ↓
   Ollama API (localhost:11434)
        ↓
   Mistral AI Model
        ↓
   "আপনি ছবি রূপান্তর, পটভূমি অপসারণ..."
```

## 📊 RAM ব্যবহার (8GB Contabo VPS)

| কম্পোনেন্ট | ব্যবহার | নোট |
|-----------|---------|------|
| Linux OS | 1-2 GB | সিস্টেম, Nginx |
| Flask App | 0.5 GB | Web framework |
| Ollama (Mistral) | 4-5 GB | AI মডেল (loading এ) |
| Free/Buffer | 1-2 GB | ভালো performance |
| **Total** | **~7 GB** | ✅ **Safe!** |

## 🔧 Chatbot Customization

আপনার সাইটের তথ্য অনুযায়ী customize করতে:

```python
# blueprints/chatbot_api.py এ এই অংশ সম্পাদনা করুন:

SYSTEM_PROMPT = """You are a helpful assistant for CutCompress...
# এখানে আপনার সাইটের তথ্য যোগ করুন
"""
```

## 🚨 ট্রাবলশুটিং

### Chatbot কাজ করছে না?

```bash
# ১. Ollama চেক করুন
curl http://localhost:11434/api/tags

# ২. Flask লগ দেখুন
tail -f /var/log/compress.log

# ৩. সেবা পুনরায় শুরু করুন
supervisorctl restart compress
supervisorctl restart ollama
```

### RAM পূর্ণ?

```bash
# মেমোরি ব্যবহার দেখুন
free -h

# Swap যোগ করুন (2GB)
dd if=/dev/zero of=/swapfile bs=1G count=2
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile
```

### মডেল ডাউনলোড ধীর?

```bash
# Ollama logs দেখুন
tail -f /var/log/ollama.log

# বা আরো হালকা মডেল ব্যবহার করুন
ollama pull neural-chat  # অনেক দ্রুত!
```

## 🎓 Ollama মডেল অপশন

```bash
# ছোট, দ্রুত (৪ GB)
ollama pull neural-chat

# মাঝারি, ভারসাম্যপূর্ণ (৭ GB) ← সুপারিশ করা
ollama pull mistral

# বড়, সবচেয়ে সঠিক (১৩ GB)
ollama pull llama2

# অতি দ্রুত, চ্যাটের জন্য অপটিমাইজড
ollama pull orca-mini
```

## 📈 Performance টিপস

1. **Caching যোগ করুন** - সাধারণ প্রশ্নের উত্তর cache করুন
2. **Redis ব্যবহার করুন** - সেশন এবং কোয়েরি caching এর জন্য
3. **CDN যোগ করুন** - Static ফাইলের জন্য (Cloudflare)
4. **Gunicorn ব্যবহার করুন** - একাধিক worker এর সাথে

```bash
pip install gunicorn redis
gunicorn -w 4 -b 127.0.0.1:5000 wsgi:application
```

## 🔒 নিরাপত্তা

- ✅ Ollama শুধুমাত্র localhost এ শোনে (বাহ্যিক অ্যাক্সেস নেই)
- ✅ API key সুরক্ষা যোগ করতে পারেন (প্রয়োজনীয়)
- ✅ Rate limiting যোগ করুন (spam প্রতিরোধ করতে)

## 📞 সাহায্য প্রয়োজন?

- সাইট: https://cutcompress.com
- ইমেইল: smartsamir0205@gmail.com
- Issues: GitHub তে report করুন

---

🎉 **এটাই! আপনার সাইটে এখন একটি স্মার্ট AI সহায়ক আছে!**
